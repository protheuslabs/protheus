name: Release GA

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  packages: write
  attestations: write
  id-token: write

jobs:
  release:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: "npm"

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-musl

      - name: Install dependencies
        run: npm ci

      - name: Resolve semantic release plan (conventional commits)
        id: semver
        run: |
          node tests/client-memory-tools/release_semver_contract.test.js > /tmp/release-lane.json
          cat /tmp/release-lane.json
          cat > /tmp/release-plan.json <<'JSON'
          {"release_ready":false,"next_tag":"none","previous_tag":"none","mode":"legacy_wrapper"}
          JSON
          cat /tmp/release-plan.json
          echo "release_ready=0" >> "$GITHUB_OUTPUT"
          echo "tag=none" >> "$GITHUB_OUTPUT"
          echo "previous_tag=none" >> "$GITHUB_OUTPUT"

      - name: Stop when no releasable conventional commits
        if: steps.semver.outputs.release_ready != '1'
        run: |
          echo "No conventional commit requiring a release."
          exit 0

      - name: Setup Nix
        if: steps.semver.outputs.release_ready == '1'
        uses: DeterminateSystems/nix-installer-action@main

      - name: Install cargo-dist and musl toolchain
        if: steps.semver.outputs.release_ready == '1'
        run: |
          cargo install cargo-dist --locked --version 0.28.0
          sudo apt-get update
          sudo apt-get install -y musl-tools

      - name: Build release binaries
        if: steps.semver.outputs.release_ready == '1'
        run: |
          set -euo pipefail
          cargo build --release --manifest-path core/layer0/ops/Cargo.toml --bin protheus-ops
          cargo build --release --target x86_64-unknown-linux-musl --manifest-path core/layer0/ops/Cargo.toml --bin protheusd
          cargo build --release -p conduit --bin conduit_daemon
          strip target/x86_64-unknown-linux-musl/release/protheusd || true
          nix --extra-experimental-features "nix-command flakes" shell nixpkgs#coreutils --command \
            dist --version
          mkdir -p client/runtime/local/state/release/artifacts
          cp target/x86_64-unknown-linux-musl/release/protheusd \
            client/runtime/local/state/release/artifacts/protheusd-x86_64-unknown-linux-musl
          npm pack --silent

      - name: Enforce static protheusd size gate (35 MB)
        if: steps.semver.outputs.release_ready == '1'
        id: size_gate
        run: |
          set -euo pipefail
          BIN="client/runtime/local/state/release/artifacts/protheusd-x86_64-unknown-linux-musl"
          SIZE_BYTES="$(wc -c < "$BIN" | tr -d ' ')"
          SIZE_MB="$(awk -v b="$SIZE_BYTES" 'BEGIN { printf "%.2f", b/(1024*1024) }')"
          MAX_BYTES=$((35 * 1024 * 1024))
          echo "protheusd_static_size_bytes=${SIZE_BYTES}" >> "$GITHUB_OUTPUT"
          echo "protheusd_static_size_mb=${SIZE_MB}" >> "$GITHUB_OUTPUT"
          if [ "$SIZE_BYTES" -gt "$MAX_BYTES" ]; then
            echo "::error::protheusd static musl binary is ${SIZE_MB} MB, exceeds the 35 MB CI gate."
            exit 1
          fi
          cargo run --quiet --manifest-path core/layer0/ops/Cargo.toml --bin protheus-ops -- \
            top1-assurance size-gate --strict=1 \
            --binary-path=client/runtime/local/state/release/artifacts/protheusd-x86_64-unknown-linux-musl \
            --max-mb=35
          echo "protheusd static binary: ${SIZE_MB} MB (musl)"

      - name: Append static binary size to release notes
        if: steps.semver.outputs.release_ready == '1'
        run: |
          printf '\nprotheusd static binary: %s MB (musl)\n' \
            "${{ steps.size_gate.outputs.protheusd_static_size_mb }}" \
            >> client/runtime/local/state/release/CHANGELOG.auto.md

      - name: Install Syft
        if: steps.semver.outputs.release_ready == '1'
        run: |
          curl -sSfL https://raw.githubusercontent.com/anchore/syft/main/install.sh | sh -s -- -b /usr/local/bin
          syft version

      - name: Generate SPDX SBOM artifacts
        if: steps.semver.outputs.release_ready == '1'
        run: |
          mkdir -p client/runtime/local/state/release/sbom
          PKG=$(ls -1 *.tgz | head -n 1)
          syft file:target/release/protheus-ops -o spdx-json=client/runtime/local/state/release/sbom/protheus-ops.spdx.json
          syft file:client/runtime/local/state/release/artifacts/protheusd-x86_64-unknown-linux-musl -o spdx-json=client/runtime/local/state/release/sbom/protheusd.spdx.json
          syft file:target/release/conduit_daemon -o spdx-json=client/runtime/local/state/release/sbom/conduit_daemon.spdx.json
          syft file:${PKG} -o spdx-json=client/runtime/local/state/release/sbom/protheus-npm.spdx.json

      - name: Sign release artifacts
        if: steps.semver.outputs.release_ready == '1'
        env:
          RELEASE_SIGNING_PRIVATE_KEY: ${{ secrets.RELEASE_SIGNING_PRIVATE_KEY }}
        run: |
          set -euo pipefail
          mkdir -p client/runtime/local/state/release/signatures
          if [ -n "${RELEASE_SIGNING_PRIVATE_KEY:-}" ]; then
            printf '%s\n' "$RELEASE_SIGNING_PRIVATE_KEY" > /tmp/release_private.pem
          else
            openssl genrsa -out /tmp/release_private.pem 4096
          fi
          openssl rsa -in /tmp/release_private.pem -pubout -out client/runtime/local/state/release/signatures/release_public.pem
          for f in target/release/protheus-ops client/runtime/local/state/release/artifacts/protheusd-x86_64-unknown-linux-musl target/release/conduit_daemon client/runtime/local/state/release/sbom/protheus-ops.spdx.json client/runtime/local/state/release/sbom/protheusd.spdx.json client/runtime/local/state/release/sbom/conduit_daemon.spdx.json client/runtime/local/state/release/sbom/protheus-npm.spdx.json; do
            base=$(basename "$f")
            openssl dgst -sha256 -sign /tmp/release_private.pem -out "client/runtime/local/state/release/signatures/${base}.sig" "$f"
          done

      - name: Create release tag
        if: steps.semver.outputs.release_ready == '1'
        run: |
          TAG="${{ steps.semver.outputs.tag }}"
          if git rev-parse "$TAG" >/dev/null 2>&1; then
            echo "Tag ${TAG} already exists."
          else
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git tag "${TAG}"
            git push origin "${TAG}"
          fi

      - name: Publish GA release
        if: steps.semver.outputs.release_ready == '1'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.semver.outputs.tag }}
          name: Protheus ${{ steps.semver.outputs.tag }}
          draft: false
          generate_release_notes: true
          body_path: client/runtime/local/state/release/CHANGELOG.auto.md
          files: |
            target/release/protheus-ops
            client/runtime/local/state/release/artifacts/protheusd-x86_64-unknown-linux-musl
            target/release/conduit_daemon
            *.tgz
            client/runtime/local/state/release/sbom/protheus-ops.spdx.json
            client/runtime/local/state/release/sbom/protheusd.spdx.json
            client/runtime/local/state/release/sbom/conduit_daemon.spdx.json
            client/runtime/local/state/release/sbom/protheus-npm.spdx.json
            client/runtime/local/state/release/signatures/protheus-ops.sig
            client/runtime/local/state/release/signatures/protheusd-x86_64-unknown-linux-musl.sig
            client/runtime/local/state/release/signatures/conduit_daemon.sig
            client/runtime/local/state/release/signatures/protheus-ops.spdx.json.sig
            client/runtime/local/state/release/signatures/protheusd.spdx.json.sig
            client/runtime/local/state/release/signatures/conduit_daemon.spdx.json.sig
            client/runtime/local/state/release/signatures/protheus-npm.spdx.json.sig
            client/runtime/local/state/release/signatures/release_public.pem
