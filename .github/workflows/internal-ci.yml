name: internal-ci

on:
  workflow_dispatch:
  push:
    paths:
      - 'crates/fluxlattice/**'
      - 'packages/lensmap/**'
      - 'systems/ops/fluxlattice_program.*'

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Placeholder
        run: echo "Internal CI placeholder for FluxLattice/LensMap"
