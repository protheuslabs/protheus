name: layer-rulebook

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  enforce-layer-rulebook:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Run layer rulebook gate
        run: tests/tooling/scripts/ci/layer_rulebook_check.sh
