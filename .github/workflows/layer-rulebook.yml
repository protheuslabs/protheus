name: layer-rulebook

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  enforce-layer-rulebook:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run layer rulebook gate
        run: client/tools/layer_rulebook_check.sh
