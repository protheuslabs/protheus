# Contributing

- Keep default `lens_mode` as `hidden`.
- Do not write private lens entries into tracked public files unless `expose` is explicitly invoked.
- Keep imports/sync operations reversible by emitting diff receipts.
