# Apps

Local-first application tools that run on top of the Protheus client surface.

## Restored tools

- `apps/pqts/` — restored from local workspace archive (`client/local/workspaces/pqts`).
- `apps/comment-matrix/` — restored from comment-mapper demo lineage.

## Policy

- `apps/` is default-ignored for ad-hoc local apps.
- Restored/approved apps are explicitly unignored in the root `.gitignore`.
