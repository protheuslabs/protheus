# chat_starter

Thin first-party chat starter app surface over Rust core `app-plane`.

## Commands

- `protheus app run chat-starter "hello"`
- `protheus app history chat-starter --session-id=<id>`
- `protheus app replay chat-starter --session-id=<id> --turn=<n>`
