# code_engineer

Thin code generation reference app surface over Rust core `app-plane`.

## Commands

- `protheus app run code-engineer "build a task tracker API"`
- `protheus app status code-engineer`
