# chat_ui

Thin multi-provider chat UI reference surface over Rust core `app-plane`.

## Commands

- `protheus app run chat-ui "summarize this"`
- `protheus app switch-provider chat-ui --provider=anthropic --model=claude-sonnet`
- `protheus app history chat-ui --session-id=<id>`
