# content-skill-graph

Workspace-local graph surface for skill/content relationship modeling.

This directory is intentionally scaffolded for `V6-SKILL-001` and can host
node/edge exports, embeddings, and compaction snapshots as the skill graph
pipeline is expanded.
