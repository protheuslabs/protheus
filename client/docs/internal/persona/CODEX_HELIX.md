# codex.helix Alias

Canonical internal source: `codex.helix` (root).

This alias exists to keep internal orchestration-profile artifacts grouped under `client/docs/internal/persona/` for public-surface clarity while preserving root runtime compatibility.
