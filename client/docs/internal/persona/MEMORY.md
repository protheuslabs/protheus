# MEMORY Alias

Canonical internal source: `MEMORY.md` (root).

This alias exists to keep internal memory-governance artifacts grouped under `client/docs/internal/persona/` for public-surface clarity while preserving root runtime compatibility.
