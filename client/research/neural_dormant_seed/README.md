# Neural Dormant Seed (Research Only)

This track is intentionally locked for non-simulated runtime activation.

## Scope

- Research-first design for future symbiosis interfaces.
- No production activation path.
- Simulation-only experiments with explicit governance receipts.

## Current State

- Feature lock: enabled (`client/config/neural_dormant_seed_policy.json` -> `locked=true`).
- Non-simulated prototypes: disabled.
- Production profiles blocked: `prod`, `default`, `phone_seed`, `live`.

## Required Before Any Non-Simulated Prototype

See `research/neural_dormant_seed/governance_checklist.md`.
