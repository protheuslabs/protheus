# Fortune 100 Execution Backlog

**Non-canonical narrative document only.**

This file is for human-readable strategy and sequencing.
**Do not use it for task tracking.**

Status, ownership, dependencies, and completion are canonical in [SRS.md](/Users/jay/.openclaw/workspace/SRS.md).
Generated execution/status views are canonical in:
- [priority_queue.md](/Users/jay/.openclaw/workspace/docs/backlog_views/priority_queue.md)
- [execution_path.md](/Users/jay/.openclaw/workspace/docs/backlog_views/execution_path.md)
- [reviewed.md](/Users/jay/.openclaw/workspace/docs/backlog_views/reviewed.md)

Purpose: raise Protheus to Fortune 100 operating standards without shrinking the capability graph or weakening the portable layered architecture.

## North Star

Protheus should behave like an AI-native operating system substrate:

- Rust remains the kernel and portability anchor.
- Conduit remains the only sanctioned bridge into flexible TS surfaces.
- Capabilities stay broad, but supportability becomes explicit by profile, layer, maturity, and owner.
- Every supported feature must have a contract, owner, test path, packaging path, and rollback path.

## Standards Target

This program is complete when all of the following are true:

1. `npm run test`, `npm run test:ci`, `npm run test:vitest`, `npm run js:holdout:audit`, and `npm run ops:ci-quality:check` are green together.
2. A normal operator workflow does not dirty tracked files outside approved generated artifacts.
3. Every command and major capability maps to one manifest with `owner`, `maturity`, `layer_floor`, `profiles`, `risk_tier`, `state_writes`, and `tests`.
4. Production entrypoints run from deterministic dist artifacts or Rust binaries, not ad hoc source bootstrap paths.
5. Supported runtime profiles are generated from layer-aware manifests rather than tribal knowledge.

## Workstreams

1. Release truth
2. Capability manifest and profile system
3. Module archetype enforcement
4. Runtime/output boundary cleanup
5. Packaging and profile distribution
6. Ownership, SLO, and release governance

## 12-Week Backlog

### Weeks 1-2: Stop contract drift

1. Align wrapper-contract policy, tests, and implementation so conduit-wrapper modules have one enforced shape.
2. Make the CI harness and local test commands agree on the same release truth.
3. Clear strict JS holdout failures and classify every remaining `.js` file as `runtime_bridge`, `compat`, or `migration debt`.
4. Add one red/green dashboard document for release truth instead of relying on scattered command outputs.

### Weeks 3-4: Introduce capability manifests

1. Add a capability manifest schema and registry generator patterned after the command registry.
2. Generate operator docs, security inventories, and profile support matrices from manifests.
3. Add maturity classes: `ga`, `beta`, `experimental`, `compat`, `retired`.
4. Add explicit `layer_floor` and `profile_allowlist` fields for every capability.

### Weeks 5-6: Make layers and profiles the product boundary

1. Define supported profiles: `seed`, `edge`, `desktop`, `cluster`, `embedded`.
2. Map `kernel_layers` features to those profiles and generate a profile matrix artifact.
3. Require every capability to declare runtime budgets and unsupported profiles.
4. Add profile boot smoke tests and packaging checks.

### Weeks 7-8: Enforce coherent module archetypes

1. Define allowed module archetypes: `primitive`, `controller`, `store`, `adapter`, `wrapper`, `tool`, `compat`, `registry`.
2. Add guardrails for allowed imports, allowed state writes, and required exports per archetype.
3. Move compatibility logic out of ambiguous runtime lanes and into explicit compat wrappers.
4. Reduce accidental direct file-system mutation from general `systems/*` modules.

### Weeks 9-10: Clean runtime/output boundaries

1. Move all runtime-emitted files out of tracked source paths unless explicitly generated by sync commands.
2. Separate generated docs from runtime state and require explicit regeneration commands.
3. Make `dist` vs `source` mode a hard environment split with the production path favoring dist only.
4. Add a repo-cleanliness gate for tracked mutable artifacts.

### Weeks 11-12: Enterprise operating model

1. Expand ownership beyond broad top-level paths.
2. Add SLOs for release truth, CI stability, packaging success, and profile boot success.
3. Add release rings and rollback policy for `ga` surfaces.
4. Freeze for a hardening pass, then run gameday, DR, and install-path validation on each supported profile.

## First 15 Concrete Tasks

1. Fix the wrapper contract mismatch between [tests/vitest/conduit_primitives_gap_closer.test.ts](/Users/jay/.openclaw/workspace/tests/vitest/conduit_primitives_gap_closer.test.ts), [config/rust_source_of_truth_policy.json](/Users/jay/.openclaw/workspace/config/rust_source_of_truth_policy.json), and [lib/direct_conduit_lane_bridge.js](/Users/jay/.openclaw/workspace/lib/direct_conduit_lane_bridge.js). Decide whether these modules are true conduit wrappers or regular modules, then enforce one answer everywhere.
2. Update [package.json](/Users/jay/.openclaw/workspace/package.json) and [memory/tools/tests/run_ci_suite.js](/Users/jay/.openclaw/workspace/memory/tools/tests/run_ci_suite.js) so `test`, `test:ci`, and `test:vitest` represent a coherent hierarchy instead of partially overlapping truths.
3. Clear the strict failures in [systems/ops/js_holdout_audit.ts](/Users/jay/.openclaw/workspace/systems/ops/js_holdout_audit.ts) by either adding TS peers or registering justified compat exceptions in [config/js_exception_registry.json](/Users/jay/.openclaw/workspace/config/js_exception_registry.json) for [systems/ops/f100_enterprise_baseline_gate.js](/Users/jay/.openclaw/workspace/systems/ops/f100_enterprise_baseline_gate.js) and [systems/ops/good_first_issue_seed.js](/Users/jay/.openclaw/workspace/systems/ops/good_first_issue_seed.js).
4. Create a single release-truth scorecard document generated from [systems/ops/ci_quality_scorecard.ts](/Users/jay/.openclaw/workspace/systems/ops/ci_quality_scorecard.ts), [systems/ops/command_registry_surface_contract.ts](/Users/jay/.openclaw/workspace/systems/ops/command_registry_surface_contract.ts), and [systems/ops/entrypoint_runtime_contract.ts](/Users/jay/.openclaw/workspace/systems/ops/entrypoint_runtime_contract.ts).
5. Add a capability manifest schema under `config/contracts/` and a generator under `systems/ops/` that mirrors the pattern already used by [config/command_registry_policy.json](/Users/jay/.openclaw/workspace/config/command_registry_policy.json) and [config/command_registry.json](/Users/jay/.openclaw/workspace/config/command_registry.json).
6. Generate a new capability inventory doc in `docs/ops/` that lists `owner`, `maturity`, `layer_floor`, `profiles`, `risk_tier`, and `state_writes` for every top-level capability family.
7. Add a profile manifest file that maps `seed`, `edge`, `desktop`, `cluster`, and `embedded` onto the existing layer features in [crates/kernel_layers/Cargo.toml](/Users/jay/.openclaw/workspace/crates/kernel_layers/Cargo.toml) and [crates/kernel_layers/src/lib.rs](/Users/jay/.openclaw/workspace/crates/kernel_layers/src/lib.rs).
8. Extend [ARCHITECTURE.md](/Users/jay/.openclaw/workspace/ARCHITECTURE.md) and [docs/GETTING_STARTED.md](/Users/jay/.openclaw/workspace/docs/GETTING_STARTED.md) with profile-aware install and support statements so operators know what runs where.
9. Add a module-archetype policy file and a guard that enforces allowed imports and required exports for `primitive`, `controller`, `store`, `adapter`, `wrapper`, `tool`, and `compat` modules. Start by covering `systems/primitives`, `systems/assimilation`, and `systems/autonomy`.
10. Update [docs/ARCHITECTURE_OWNERSHIP.md](/Users/jay/.openclaw/workspace/docs/ARCHITECTURE_OWNERSHIP.md) so ownership is expressed in terms of archetypes, manifests, and write permissions, not only directories.
11. Move runtime-emitted mutable outputs out of tracked source locations by auditing the files currently dirty under `adaptive/`, `config/`, and `docs/`, then routing runtime writes into `state/` or `STATE_ROOT/`.
12. Tighten the production/runtime split in [lib/ts_bootstrap.js](/Users/jay/.openclaw/workspace/lib/ts_bootstrap.js) and [config/entrypoint_runtime_contract_policy.json](/Users/jay/.openclaw/workspace/config/entrypoint_runtime_contract_policy.json) so dist-first execution is mandatory for production profiles.
13. Add profile-aware smoke suites to CI and wire them into [.github/workflows/required-checks.yml](/Users/jay/.openclaw/workspace/.github/workflows/required-checks.yml). At minimum: `layer0`, `layer2`, `layer3`, plus one dist-entrypoint smoke.
14. Replace broad ownership with subsystem ownership by expanding [.github/CODEOWNERS](/Users/jay/.openclaw/workspace/.github/CODEOWNERS) around `conduit`, `kernel`, `security`, `memory`, `autonomy`, `ops`, and packaging paths.
15. Raise the CI quality bar in [config/ci_quality_scorecard_policy.json](/Users/jay/.openclaw/workspace/config/ci_quality_scorecard_policy.json) only after the suite is stable: first make flake and runtime budgets honest, then ratchet thresholds upward and tie them to release gating.

## Second 15 Concrete Tasks To Reach `A`

16. Add a generated `supported surface matrix` artifact that combines capability manifests, command registry metadata, and profile manifests into one operator truth. Store the generated artifact under `state/ops/` and publish a human-readable summary under `docs/ops/`.
17. Introduce `ga admission criteria` for capabilities: required owner, profile declarations, rollback path, package inclusion, install validation, runbook, and a passing contract suite. Encode the policy under `config/` and gate it in CI under `systems/ops/`.
18. Add a `profile packaging contract` that proves each supported profile produces a deterministic installable bundle. Wire checks against [deploy/helm/protheus/Chart.yaml](/Users/jay/.openclaw/workspace/deploy/helm/protheus/Chart.yaml), [deploy/terraform/protheus_helm/main.tf](/Users/jay/.openclaw/workspace/deploy/terraform/protheus_helm/main.tf), npm packaging, and direct binary install surfaces.
19. Add dist artifact verification for `protheus`, `protheusctl`, `protheusd`, and `protheus-top` so [systems/ops/entrypoint_runtime_contract.ts](/Users/jay/.openclaw/workspace/systems/ops/entrypoint_runtime_contract.ts) fails if production profile targets are missing built artifacts instead of accepting source fallback alone.
20. Create `profile support statements` in [docs/GETTING_STARTED.md](/Users/jay/.openclaw/workspace/docs/GETTING_STARTED.md) and [ARCHITECTURE.md](/Users/jay/.openclaw/workspace/ARCHITECTURE.md) that define what `seed`, `edge`, `desktop`, `cluster`, and `embedded` guarantee and explicitly exclude.
21. Add deterministic install-path tests for macOS/Linux shell install, PowerShell install, npm wrapper install, and direct Cargo binary execution. These should validate the actual operator path, not just file presence.
22. Split the command registry into `supported`, `internal`, `experimental`, and `compat` sections while keeping a single generated source of truth in [config/command_registry.json](/Users/jay/.openclaw/workspace/config/command_registry.json). This preserves breadth while making operator expectations explicit.
23. Add `owner escalation depth` to command and capability metadata so every high-risk subsystem has primary, secondary, and release-owner coverage instead of one broad path owner in [.github/CODEOWNERS](/Users/jay/.openclaw/workspace/.github/CODEOWNERS).
24. Add `human-only action coverage` validation that cross-checks high-risk commands and workflows against [docs/HUMAN_ONLY_ACTIONS.md](/Users/jay/.openclaw/workspace/docs/HUMAN_ONLY_ACTIONS.md), ensuring autonomous paths never silently bypass human-required boundaries.
25. Add a `profile boot certification` workflow that boots each supported profile, runs minimal kernel checks, validates conduit if present, and emits a signed receipt. This is the right enterprise analog for “runs on anything, including a toaster.”
26. Move from directory-based architecture ownership to `write-scope ownership` and `runtime authority ownership`. Expand [docs/ARCHITECTURE_OWNERSHIP.md](/Users/jay/.openclaw/workspace/docs/ARCHITECTURE_OWNERSHIP.md) so teams own contracts, mutation channels, and release surfaces, not just folders.
27. Add a `generated artifact discipline` policy: docs like [docs/ops/F100_ENTERPRISE_BASELINE_STATUS.md](/Users/jay/.openclaw/workspace/docs/ops/F100_ENTERPRISE_BASELINE_STATUS.md) and command/security inventories must only change via explicit sync commands, never as incidental runtime side effects.
28. Add release-note and changelog completeness gates that ensure every `ga` or `beta` capability change updates [CHANGELOG.md](/Users/jay/.openclaw/workspace/CHANGELOG.md) and links to the governing manifest, tests, and rollout profile.
29. Add a `performance and footprint budget matrix` by profile. Fortune 100 quality for a portable substrate means you can answer memory, startup, and runtime cost questions for `layer0` through `layer3`, not just functional correctness.
30. Add a quarterly `enterprise readiness review pack` generator that assembles release truth, ownership coverage, packaging status, security inventory, install-path evidence, and profile certifications into one consumable review bundle for leadership or customers.

## Sequencing Rules

1. Do not start capability-manifest work until release truth is green.
2. Do not tighten SLO thresholds until CI noise is under control.
3. Do not move modules between archetypes without updating the manifest and the guard policy in the same change.
4. Do not remove compatibility layers until profile packaging and rollback paths exist.

## Weekly Success Metrics

1. Number of red release-truth gates
2. Number of uncategorized capabilities
3. Number of uncategorized JS holdouts
4. Number of tracked runtime-mutated files
5. Number of supported profiles with green boot smoke
6. Mean time to explain ownership/support status for any command or capability

## Desired End State

Protheus remains broad, ambitious, and portable, but it stops behaving like a growing research tree and starts behaving like a productized substrate:

- capabilities preserved
- support boundaries explicit
- profiles first-class
- contracts generated from one truth
- runtime outputs isolated
- enterprise confidence earned by repeatable evidence
