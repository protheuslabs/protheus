# Memory Migration Status

## Overview

This document tracks the staged Rust memory migration for `V3-RACE-023`.

## Stage Status

- Stage 1: COMPLETE (2026-03-01)
- Stage 2: COMPLETE (2026-03-01)
- Stage 3: COMPLETE (2026-03-01)
- Stage 4: Pending

## Stage 1 Scope (Completed)

- Rust crate moved to `systems/memory/rust` and renamed to `protheus-memory-core`.
- SQLite is now the authoritative runtime index store for Rust memory commands.
- SQLite schema includes:
  - `embeddings`
  - `temporal_graph_nodes`
  - `temporal_graph_edges`
  - `hot_state`
  - `memory_index`
- Runtime commands (`query-index`, `get-node`, `build-index`) use SQLite-first behavior.
- Markdown index files remain compatibility fallback artifacts.
- TypeScript integration (`systems/memory/memory_recall.ts`) defaults to Rust first with deterministic JS fallback.
- Memory write receipts are published via `systems/ops/event_sourced_control_plane.js`.
- Stage 1 benchmark report is generated at `benchmarks/memory-stage1.md` from `systems/memory/rust_memory_transition_lane.ts`.

## Stage 1 Safety Notes

- Fallback path remains active for rollback safety.
- Benchmark-gated selector logic remains intact in transition lane.
- Stage 1 intentionally preserved markdown fallback compatibility before Stage 2 cutover work.

## Stage 2 Scope (Completed)

- Runtime Rust reads no longer depend on `MEMORY_INDEX.md`/`TAGS_INDEX.md`.
- SQLite index refresh now derives from authoritative daily node scans (`memory/YYYY-MM-DD.md` sections).
- Markdown index files are treated as export/human artifacts, not hot-path runtime inputs.
- Rust runtime still retains deterministic compatibility fallback paths for safety.
- Stage benchmark report emission remains in `benchmarks/memory-stage1.md` (generated by transition lane).

## Stage 3 Scope (Completed)

- Added long-lived Rust daemon mode (`daemon` command) in `systems/memory/rust/src/main.rs`.
- Daemon serves JSON RPC over localhost (`query-index`, `get-node`, `build-index`, `probe`, `ping`, `shutdown`).
- `systems/memory/memory_recall.ts` now uses daemon-first transport for Rust backend in main workspace sessions.
- Added daemon auto-start with PID/log state under `state/memory/rust_transition/`.
- Added deterministic fallback to existing Rust CLI path, then JS path, so failures stay safe.
- Added `rust_transport` response field (`daemon` or `cli`) for runtime verification.
- Test guard: daemon mode is disabled for temp-root test workspaces to preserve deterministic fixture behavior.
