# 2026-03-13

- Implemented `core/layer0/ops/src/seed_protocol.rs` as Rust-authoritative V9 seed runtime with deploy/migrate/enforce/select/archive/defend/status commands and deterministic receipts.
- Wired seed runtime into `protheus-ops` domain dispatch and `protheusctl` core shortcuts (`protheus seed ...`), including normalization for `seed ignite viral` -> `deploy --profile=viral`.
- Added behavior and integration coverage:
  - unit tests in `seed_protocol.rs`
  - CLI routing tests in `protheusctl.rs`
  - integration test `core/layer0/ops/tests/v9_seed_protocol_integration.rs`
- Added V9 SRS contracts under `planes/contracts/srs/V9-*.json` and executed runtime receipts via `protheus-ops srs-contract-runtime run-many --ids=...`.
- Updated SRS ledgers to mark `V9-VIRAL-001.1..001.6` and `V9-IMMORTAL-001.1..001.6` as `done`.
- Environment blocker: `node`/`npm` are unavailable in this runtime, so JS-based repo gates (`ops:srs:full:regression`, `ops:dod:gate`, `test:ops:srs-contract-runtime-evidence`, `verify.sh`) cannot be executed here.

- Implemented Batch 5 high-impact lanes in Rust core:
  - `core/layer0/ops/src/asm_plane.rs` for `V7-ASM-004..010`
  - `core/layer0/ops/src/research_plane.rs` for `V6-RESEARCH-001.1..001.3`
  - `core/layer1/security/src/security_wave1.rs` ABAC plane runtime (`V7-ASM-006`)
  - `core/layer0/ops/src/security_plane.rs` dispatch wiring for `abac-policy-plane`
- Added Batch 5 contract/config artifacts:
  - `planes/contracts/{wasm_dual_meter_policy_v1.json,abac_policy_plane_v1.json,crdt_automerge_profile_v1.json,trust_chain_integration_v1.json,fastpath_hotpath_policy_v1.json}`
  - `planes/contracts/hands/HAND.toml`
  - `planes/contracts/industrial/{isa95_template.json,rami40_template.json,validation_checklist.json}`
  - `planes/contracts/research/research_plane_v1.json`
  - `client/runtime/config/{abac_policy_plane.json,research_plane_policy.json}`
- Added behavior/integration coverage:
  - `core/layer1/security/src/security_wave1.rs` ABAC unit tests
  - `core/layer0/ops/tests/v7_asm_research_batch5_integration.rs`
- Evidence:
  - `cargo test -p infring-layer1-security abac_policy_plane_tests`
  - `cargo test -p protheus-ops-core --test v7_asm_research_batch5_integration`
  - `cargo test -p protheus-ops-core -- --test-threads=1` (full package pass)
  - CLI evidence file: `artifacts/v7_asm_research_batch5_cli_evidence_20260313T100405Z.jsonl`
- `verify.sh` remains blocked at JS gate stage in this environment due missing `npm` binary.

- Implemented Batch 6 research lanes in Rust core with strict behavior + receipts (`V6-RESEARCH-001.4..001.6`, `V6-RESEARCH-002.1..002.6`).
- Added `core/layer0/ops/src/research_batch6.rs` and wired commands into `research_plane` (`diagnostics`, `mcp-extract`, `spider`, `middleware`, `pipeline`, `signals`, `console`, `template-governance`) with safety-plane gating and deterministic receipts.
- Enforced core-first user path for research by routing `protheus research` to `core://research-plane` in `protheusctl_routes` and added routing regression tests.
- Added research contract pack under `planes/contracts/research/` plus curated template fixtures and governance manifest.
- Added strict integration coverage: `core/layer0/ops/tests/v6_research_batch6_integration.rs`.
- Verified with targeted tests and produced runnable CLI evidence artifact: `artifacts/v6_research_batch6_cli_evidence_20260313T103510Z.jsonl`.

- Implemented Batch 7 research stack in Rust core (`V6-RESEARCH-004.1..004.6`, `V6-RESEARCH-005.1..005.3`, `V6-RESEARCH-006.1`) via new `core/layer0/ops/src/research_batch7.rs` and routed commands through `research_plane`.
- Added top-level core shortcut routing to research core lane for `protheus crawl`, `protheus map`, and `protheus monitor` in `protheusctl_routes.rs` with regression tests.
- Added Batch 7 contracts and firecrawl curated template governance artifacts in `planes/contracts/research/`.
- Added strict integration coverage in `core/layer0/ops/tests/v6_research_batch7_integration.rs` including conduit bypass fail-closed test.
- Produced CLI evidence: `artifacts/v6_research_batch7_cli_evidence_20260313T105110Z.jsonl`.
- Implemented Batch 8 core closures in Rust authority:
  - Added `core/layer0/ops/src/research_batch8.rs` covering `V6-RESEARCH-005.4/005.5/005.6` and `V6-RESEARCH-006.2/006.3/006.4/006.5/006.6`.
  - Added `core/layer0/ops/src/parse_plane.rs` covering `V6-PARSE-001.1/001.2`.
  - Wired new commands through `research_plane`, `main`, `ops_main_usage`, and `protheusctl` top-level routes (`protheus parse ...`).
  - Added integration tests: `v6_research_batch8_integration.rs`, `v6_parse_batch8_integration.rs`, plus route tests for parse shortcuts.
  - Generated runnable CLI evidence: `artifacts/v6_research_parse_batch8_cli_evidence_20260313T111439Z.jsonl`.
- Implemented Batch 9 production lanes in Rust core:
  - Extended `parse-plane` with `postprocess-table`, `flatten`, `template-governance`, and strict conduit-only fail-closed enforcement (`V6-PARSE-001.3..001.6`).
  - Added new `mcp-plane` core domain with `capability-matrix`, durable `workflow` pause/resume/retry, `expose`, `pattern-pack`, `template-governance`, and strict conduit-only fail-closed enforcement (`V6-MCP-001.1..001.6`).
  - Wired dispatch/routes (`main`, `lib`, `ops_main_usage`, `protheusctl_routes`) including top-level `protheus mcp ...` shortcuts.
  - Added contracts under `planes/contracts/parse/` and `planes/contracts/mcp/`.
  - Added integration suites `v6_parse_batch9_integration.rs` and `v6_mcp_batch9_integration.rs`; full `cargo test -p protheus-ops-core -- --test-threads=1` passed.
  - Generated CLI runtime evidence at `artifacts/v6_parse_mcp_batch9_cli_evidence_20260313T113948Z.jsonl`.

- Implemented Batch 10 FLOW + SKILLS closures in Rust core:
  - Added `core/layer0/ops/src/flow_plane.rs` and `core/layer0/ops/src/skills_plane.rs` with conduit-only fail-closed enforcement and deterministic receipts for `V6-FLOW-001.1..001.6` and `V6-SKILLS-001.1..001.4`.
  - Wired domains through `lib`, `main`, `ops_main_usage`, and `protheusctl` shortcut routing (`flow`, `skill`, `skills` status fallback).
  - Added contracts under `planes/contracts/flow/` and `planes/contracts/skills/`.
  - Added integration suites `core/layer0/ops/tests/v6_flow_batch10_integration.rs` and `core/layer0/ops/tests/v6_skills_batch10_integration.rs`.
  - Validation evidence:
    - Full package tests: `cargo test --manifest-path core/layer0/ops/Cargo.toml -- --test-threads=1` (374 tests pass in `protheus-ops-core` lib + integration suites).
    - Runnable CLI evidence bundle: `artifacts/v6_flow_skills_batch10_cli_evidence_20260313T120313Z.jsonl`.

- Implemented Batch 11 closures in Rust core for audit gaps:
  - Extended `core/layer0/ops/src/skills_plane.rs` to cover `V6-SKILLS-001.5..001.8` (`list`, `dashboard`, `gallery` ingest/list/load, `react-minimal`, `tot-deliberate`) with deterministic receipts and strict conduit fail-closed checks.
  - Added new Rust domains `core/layer0/ops/src/binary_vuln_plane.rs` (`V6-BINVULN-001.1..001.3`) and `core/layer0/ops/src/hermes_plane.rs` (`V6-HERMES-001.1..001.2`).
  - Wired domains through `lib/main/usage/protheusctl_routes` and added route tests for new core shortcuts (`binary-vuln`, `scan binary`, `shadow discover`, `top`, `status --dashboard`).
  - Added contract packs under `planes/contracts/skills/`, `planes/contracts/binary_vuln/`, and `planes/contracts/hermes/`.
  - Added integration suites: `v6_skills_batch11_integration.rs`, `v6_binvuln_batch11_integration.rs`, `v6_hermes_batch11_integration.rs`.
  - Generated CLI evidence bundle: `artifacts/v6_skills_binvuln_hermes_batch11_cli_evidence_20260313T123321Z.jsonl`.
  - Validation: full `cargo test --manifest-path core/layer0/ops/Cargo.toml -- --test-threads=1` passed.

- Implemented Batch 12 closures in Rust core for `V6-HERMES-001.3..001.5`, `V6-VBROWSER-001.1..001.5`, and `V6-AGENCY-001.1..001.2`:
  - Added/extended core planes: `hermes_plane` (continuity + delegation), `vbrowser_plane` (session/collab/automation/privacy), `agency_plane` (template creation + division topology).
  - Added route regressions in `protheusctl` for `browser`, `agency`, and `shadow` browser/delegate/continuity/create flows.
  - Added Batch 12 integration suites:
    - `core/layer0/ops/tests/v6_hermes_batch12_integration.rs`
    - `core/layer0/ops/tests/v6_vbrowser_batch12_integration.rs`
    - `core/layer0/ops/tests/v6_agency_batch12_integration.rs`
  - Added dashboard metric surfacing in `health_status` for vbrowser + agency and persisted health latest snapshot for testable dashboard evidence.
  - Full verification: `cargo test --manifest-path core/layer0/ops/Cargo.toml -- --test-threads=1` passed.
  - CLI evidence: `artifacts/v6_hermes_vbrowser_agency_batch12_cli_evidence_20260313T125812Z.jsonl`.

- Closed Batch 13 core implementation gaps in Rust authority:
  - Added `collab_plane` (`V6-COLLAB-001.1..001.5`) and `company_plane` (`V6-COMPANY-001.1..001.2`).
  - Extended `agency_plane` for `V6-AGENCY-001.3..001.5` (`orchestrate`, `workflow-bind`, strict conduit fail-closed).
  - Wired `protheusctl` shortcuts for `team`, `company`, and `orchestrate agency` to Layer-0 core lanes.
  - Added contracts under `planes/contracts/{agency,collab,company}/` and integration suites:
    - `v6_agency_batch13_integration.rs`
    - `v6_collab_batch13_integration.rs`
    - `v6_company_batch13_integration.rs`
- Validation:
  - Targeted tests passed: batch13 agency/collab/company integration suites.
  - Full `core/layer0/ops` test sweep passed (`cargo test --manifest-path core/layer0/ops/Cargo.toml -- --test-threads=1`).
  - CLI evidence generated:
    - `artifacts/v6_agency_collab_company_batch13_cli_evidence_20260313T131813Z.jsonl`
    - `artifacts/v6_agency_collab_company_batch13_shortcuts_20260313T131904Z.jsonl`

- Implemented Batch 14 core closures in Rust authority:
  - Extended `company_plane` with `ticket` immutable audit-chain primitives (`create/assign/transition/handoff/close/status`) for `V6-COMPANY-001.3`.
  - Added `company_plane heartbeat` runtime (`tick/status/remote-feed`) and remote monitoring artifacts for `V6-COMPANY-001.4`.
  - Enforced conduit fail-closed guardrails across all company ops (`orchestrate`, `budget`, `ticket`, `heartbeat`) for `V6-COMPANY-001.5`.
  - Added new `substrate_plane` core domain implementing `V6-SUBSTRATE-001.1..001.5` and `V6-SUBSTRATE-002.1..002.2`:
    - CSI capture + Layer -1 envelope wrapping + Layer 2 normalized inference
    - CSI module registry/activation
    - Embedded ESP32 profile with degraded-mode receipts
    - CSI privacy/consent/locality policy lane
    - Native Eye binding (`enable/status`) thin-surface mapping
    - Biological interface mapping + closed-loop feedback with silicon-only degradation
- Added contracts:
  - `planes/contracts/company/{ticket_audit_contract_v1.json,team_heartbeat_contract_v1.json}`
  - `planes/contracts/substrate/*.json` (capture, module, embedded, policy, eye, bio-interface, bio-feedback)
- Added integration tests:
  - `core/layer0/ops/tests/v6_company_batch14_integration.rs`
  - `core/layer0/ops/tests/v6_substrate_batch14_integration.rs`
- Routed core shortcuts:
  - `protheusctl company ticket|heartbeat`, top-level `ticket|heartbeat`
  - `protheusctl substrate ...`
  - `protheusctl eye enable wifi` -> core `substrate-plane eye-bind`
- Validation:
  - `cargo test --manifest-path core/layer0/ops/Cargo.toml --test v6_company_batch14_integration -- --test-threads=1` pass
  - `cargo test --manifest-path core/layer0/ops/Cargo.toml --test v6_substrate_batch14_integration -- --test-threads=1` pass
  - full `cargo test --manifest-path core/layer0/ops/Cargo.toml -- --test-threads=1` pass (409 lib tests + integration suites)
- CLI evidence:
  - `artifacts/v6_company_substrate_batch14_cli_evidence_20260313T134228Z.jsonl`
- Post-fix note: corrected company ticket chain validation to scope hash-chain checks per ticket ID (not all team events).
- Final Batch 14 CLI evidence bundle: `artifacts/v6_company_substrate_batch14_cli_evidence_20260313T134228Z.jsonl`.

- Implemented Batch 15 closures in Rust core (`V6-SUBSTRATE-002.3..002.5`, `V6-OBSERVABILITY-001.1..001.5`, `V6-PERSIST-001.1..001.2`):
  - Added core domains `observability_plane` and `persist_plane` with strict conduit fail-closed gating and deterministic receipts.
  - Extended `substrate_plane` with biological adapter template, bioethics/consent gate (blocked-external prepared logic), and biological enable lane exposed via `protheus substrate enable biological`.
  - Added health dashboard metrics for observability/persist and biological mode surfacing.
  - Added contracts under `planes/contracts/{observability,persist,substrate}` for new lanes.
  - Added integration suites:
    - `core/layer0/ops/tests/v6_substrate_batch15_integration.rs`
    - `core/layer0/ops/tests/v6_observability_batch15_integration.rs`
    - `core/layer0/ops/tests/v6_persist_batch15_integration.rs`
- Validation:
  - `cargo test --manifest-path core/layer0/ops/Cargo.toml --test v6_substrate_batch15_integration -- --test-threads=1`
  - `cargo test --manifest-path core/layer0/ops/Cargo.toml --test v6_observability_batch15_integration -- --test-threads=1`
  - `cargo test --manifest-path core/layer0/ops/Cargo.toml --test v6_persist_batch15_integration -- --test-threads=1`
  - Full suite: `cargo test --manifest-path core/layer0/ops/Cargo.toml -- --test-threads=1` (pass)
- CLI evidence: `artifacts/v6_substrate_observability_persist_batch15_cli_evidence_20260313T140830Z.jsonl`

- Implemented Batch 16 closures in Rust core (`V6-PERSIST-001.3..001.6`, `V6-APP-008.1`, `V6-APP-007.1`, `V6-APP-006.1..006.3`):
  - Extended `persist_plane` with `continuity`, `connector`, and `cowork` commands plus strict conduit fail-closed guardrails.
  - Added new `core/layer0/ops/src/app_plane.rs` for core-authoritative app execution (`chat-starter`, `chat-ui`, `code-engineer`) with deterministic receipts.
  - Added contracts:
    - `planes/contracts/persist/{continuity_contract_v1.json,connector_onboarding_contract_v1.json,cowork_background_contract_v1.json}`
    - `planes/contracts/apps/{chat_starter_contract_v1.json,chat_ui_contract_v1.json,code_engineer_contract_v1.json}`
  - Added thin app surfaces:
    - `apps/chat_starter/*`, `apps/chat_ui/*`, `apps/code_engineer/*`
  - Added integration suites:
    - `core/layer0/ops/tests/v6_persist_batch16_integration.rs`
    - `core/layer0/ops/tests/v6_apps_batch16_integration.rs`
  - Updated routing/tests:
    - `protheusctl_routes` now routes `persist continuity|connector|cowork`, top-level `connector`, `cowork`, and `app run ...` to core lanes.
  - Validation:
    - `cargo test --manifest-path core/layer0/ops/Cargo.toml --test v6_persist_batch16_integration -- --test-threads=1`
    - `cargo test --manifest-path core/layer0/ops/Cargo.toml --test v6_apps_batch16_integration -- --test-threads=1`
    - `cargo test --manifest-path core/layer0/ops/Cargo.toml core_shortcut_routes_ -- --test-threads=1`
    - full suite: `cargo test --manifest-path core/layer0/ops/Cargo.toml -- --test-threads=1` (pass)
  - CLI evidence:
    - `artifacts/v6_persist_apps_batch16_cli_evidence_20260313T143628Z.jsonl`

- Implemented Batch 17 closures in Rust core (`V6-APP-006.4..006.8`, `V6-APP-023.1..023.5`):
  - Extended `app_plane` with production builder actions: `build`, mandatory reasoning gate, Slack/Telegram ingress bridge, bounded sub-agent delivery manifest flow, and `builders://` template governance lane.
  - Added new `snowball_plane` core domain with: `start`, `melt-refine`, `compact` (sphere-of-ice snapshot), `backlog-pack`, `status`, and `control`.
  - Added top-level shortcut routing for `protheus build ...` and `protheus snowball ...` in `protheusctl_routes` plus route regression tests.
  - Added thin app shell `apps/snowball_engine/` and app contracts `product_builder_contract_v1.json`, `snowball_engine_contract_v1.json`.
- Validation:
  - `cargo test --manifest-path core/layer0/ops/Cargo.toml --test v6_app_batch17_integration -- --test-threads=1`
  - `cargo test --manifest-path core/layer0/ops/Cargo.toml core_shortcut_routes_build_goal_to_app_plane -- --test-threads=1`
  - `cargo test --manifest-path core/layer0/ops/Cargo.toml core_shortcut_routes_snowball_start_to_core_plane -- --test-threads=1`
  - `cargo test --manifest-path core/layer0/ops/Cargo.toml -- --test-threads=1` (full suite pass)
- CLI evidence:
  - `artifacts/v6_app_snowball_batch17_cli_evidence_20260313T150546Z.jsonl`
- Churn cleanup:
  - moved generated runtime outputs from `apps/code_engineer/builds/` into `.trash/` to keep tree focused on source changes.

- Implemented Batch 18 closures in Rust core (`V6-APP-023.6`, `V6-EVAL-001.1..001.4`, `V6-ECONOMY-001.1..001.4`):
  - Added new core domain `core/layer0/ops/src/eval_plane.rs` with strict conduit fail-closed enforcement, build/experiment/evaluate loop, persisted fixtures/traces, benchmark cost/accuracy deltas, and deterministic receipts.
  - Wired `eval-plane` through `lib/main/ops_main_usage/protheusctl_routes` and updated route regression tests to route `protheus eval ...` and `protheus experiment loop ...` to `core://eval-plane`.
  - Extended `snowball_plane` to emit explicit `V6-APP-023.6` evidence for start/melt/compact/backlog/control/status surfaces while preserving prior `V6-APP-023.5` coverage.
  - Hardened `llm_economy_organ` with strict conduit bypass rejection and mapped marketplace hand claims to canonical IDs (`V6-ECONOMY-001.1..001.4`).
  - Added contracts:
    - `planes/contracts/eval/eval_loop_contract_v1.json`
    - `planes/contracts/economy/economy_hands_contract_v1.json`
  - Added integration suite: `core/layer0/ops/tests/v6_eval_economy_batch18_integration.rs`.
- Validation/evidence:
  - `cargo test --manifest-path core/layer0/ops/Cargo.toml --test v6_eval_economy_batch18_integration -- --test-threads=1` (pass)
  - `cargo test --manifest-path core/layer0/ops/Cargo.toml -- --test-threads=1` (full pass)
  - CLI evidence bundle: `artifacts/v6_eval_economy_batch18_cli_evidence_20260313T152655Z.jsonl`
