---
date: 2026-03-10
node_id: client-language-format-closure
uid: M20260310A
tags: [ops, policy, client, migration]
---
# client-language-format-closure

- Removed the last tracked `.js`, `.sh`, `.py`, and `.ps1` files from `client`.
- Moved the IMAP/SMTP setup helper to `scripts/skills/imap-smtp-email/setup.sh`.
- Rewrote invalid renamed `.ts` wrapper shells into ESM-compatible TS entrypoints backed by `legacy_retired_wrapper.ts`.
- Fixed `client/runtime/systems/compat/legacy_retired_lane.ts` parsing for `--lane-id=...`.
- Verified representative runtime and cognition wrappers plus policy audits and `./verify.sh`.

<!-- NODE -->

---
date: 2026-03-10
node_id: client-boundary-audit-baseline-refresh
uid: M20260310B
tags: [ops, policy, audit, metrics]
---
# client-boundary-audit-baseline-refresh

- Replaced stale `git ls-files` client scope and debt measurement with live filesystem scans.
- Wired `client_surface_disposition.mjs` and `client_target_contract_audit.mjs` into `package.json` and `verify.sh`.
- Re-homed non-client payloads from `client` into `apps/personas/...`, `adapters/skills/...`, and top-level `tmp/` plus `state/local/`.
- Policy gates passing at checkpoint:
  - `ops:client-layer:boundary`
  - `ops:repo-surface:audit`
  - `ops:public-platform:contract`
  - `ops:client-target:audit`
  - `./verify.sh`

<!-- NODE -->

---
date: 2026-03-10
node_id: client-boundary-target-gap
uid: M20260310C
tags: [ops, policy, backlog, client]
---
# client-boundary-target-gap

- Converted allowlist entries into real client shims with authority moved into `adapters/economy/smart_contract_bridge.ts`, `adapters/importers/*`, and `apps/examples/singularity-seed-demo/orchestrator.ts`.
- Kept `bird_x` and `ollama_search` temporarily in the client allowlist because they still depend on cognition internals and need a real extraction.
- Truthful checkpoint metrics:
  - `total_ts_files=926`
  - `runtime_system_surface=687`
  - `cognition_surface=100`
  - `runtime_sdk_surface=59`
  - `wrapper_count=673`
  - `allowed_non_wrapper_count=15`
